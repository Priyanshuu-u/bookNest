{
  "projects": {
    "backend": {
      "src": "backend",
      "buildCommand": "npm install && npm run build",
      "outputDirectory": "dist"
    },
    "frontend": {
      "src": "frontend",
      "buildCommand": "npm install && npm run build",
      "outputDirectory": "build"
    }
  }
}
